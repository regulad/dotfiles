# Configuration
Include config.d/*
AddKeysToAgent yes
HashKnownHosts yes
VerifyHostKeyDNS yes

Host *
  EnableEscapeCommandline yes

# Hardware hosts
Host neodingus
    HostName neodingus.tail11540.ts.net
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host prodingus
    HostName prodingus.tail11540.ts.net
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host jondingus
    HostName jondingus.tail11540.ts.net
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host turk
    HostName turk.tail11540.ts.net
    ForwardAgent yes

# Switches
Host JPE18280417
    # Embedded
    WarnWeakCrypto no
    HostName jpe18280417.home.arpa

# Linux & macOS laptops
Host penumbra
    HostName penumbra.tail11540.ts.net
    ForwardAgent yes

# Router VMs
Host nagumo
    HostName nagumo.home.arpa
    User admin
    PubkeyAuthentication yes
    PreferredAuthentications publickey   
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent no

Host niikura
    HostName niikura.home.arpa
    User admin
    PubkeyAuthentication yes
    PreferredAuthentications publickey   
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent no

# Master VMs
Host gendo
    HostName gendo.tail11540.ts.net
    ForwardAgent yes

Host misato
    HostName misato.tail11540.ts.net
    ForwardAgent yes

# Misc.
Host pihole
    HostName pihole.home.arpa
    User root
    PubkeyAuthentication no
    PreferredAuthentications password
    ForwardAgent yes

# LXC
Host llamarpcmaster
    HostName llamarpcmaster.home.arpa
    ForwardAgent yes

Host openclaw
    HostName openclaw.home.arpa
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host timemachine
    HostName timemachine.home.arpa
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host openwebui
    HostName openwebui.home.arpa
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host immich
    HostName immich.home.arpa
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host uptime
    HostName uptime.home.arpa
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host peppercord
    HostName peppercord.home.arpa
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host cordpepperer
    HostName cordpepperer.home.arpa
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host tvheadend
    HostName tvheadend.home.arpa
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host vaultwarden
    HostName vaultwarden.home.arpa
    User root
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

# VMs
Host pelican
    HostName pelican.home.arpa

Host lancache
    HostName lancache.home.arpa

Host freeipa
    HostName freeipa.home.arpa

Host authentik
    HostName authentik.home.arpa

Host mastodon
    HostName mastodon.home.arpa

Host urbackup
    HostName urbackup.home.arpa

Host obico
    HostName obico.home.arpa

Host softether
    HostName softether.home.arpa

Host gha
    HostName gha.home.arpa

Host act
    HostName act.home.arpa

Host home-arpa--tailscalerouter
    HostName home-arpa--tailscalerouter.home.arpa

# Windows hosts are no longer included for ssh key auth.
# There is a ton of garbage with "key scopes" that prevents the use of junctions while connected.

# Oracle (Legacy VCN)
Host doppelgangerhq.com
    AddressFamily inet  # IPv6 broken
    User ubuntu
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

# Oracle (wahlecloud)
Host pubsub-wahlecloud--tailscalerouter
    HostName pubsub-wahlecloud--tailscalerouter.pubsub.wahlecloud.oraclevcn.com

Match Host pubsub-wahlecloud--tailscalerouter User opc
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/ssh-key-2025-12-08_tailscalerouter.key.pub
    IdentityFile ~/.ssh/ssh-key-2025-12-08_tailscalerouter.key  # fallback
{{- end }}
    IdentitiesOnly yes
    ForwardAgent yes

Host livekit

Match Host livekit User opc
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/ssh-key-2025-12-19_livekit.key.pub
    IdentityFile ~/.ssh/ssh-key-2025-12-19_livekit.key  # fallback
{{- end }}
    IdentitiesOnly yes

Host mail

Match Host mail User opc
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/ssh-key-2025-12-08_mail.key.pub
    IdentityFile ~/.ssh/ssh-key-2025-12-08_mail.key  # fallback
{{- end }}
    IdentitiesOnly yes

# Personal Git
Host github.com
    HostName github.com
    User git
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes

Host invent.kde.org
    HostName invent.kde.org
    User git
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes

# git.regulad.xyz (super special case)
Host git-ssh.regulad.xyz
    ProxyCommand cloudflared access ssh --hostname %h
    HostName git-ssh.regulad.xyz
    User forgejo
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes

Match Host mail User regulad
    ForwardAgent yes

# cloudflared protected hosts
Host matrix
     ProxyCommand cloudflared access ssh --hostname %h
     HostName ssh-matrix.regulad.xyz

# ncsu
Host github.ncsu.edu
    HostName github.ncsu.edu
    User git
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_ed25519_ncstate.pub
    IdentityFile ~/.ssh/id_ed25519_ncstate  # fallback
{{- end }}
    IdentitiesOnly yes

## rotating host keys and password only authentication.
## excellent security, mr. bond
Host eos
    # lab server
    HostName remote.eos.ncsu.edu
    User pewahle    
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null
    PubkeyAuthentication no
    PreferredAuthentications password

Host grendel
    # x86_64
    ForwardX11 yes
    HostName grendel.ece.ncsu.edu
    User pewahle
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null
    PubkeyAuthentication no
    PreferredAuthentications password

Host hydra
    # GPU
    ForwardX11 yes
    HostName hydra.ece.ncsu.edu
    User pewahle
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null
    PubkeyAuthentication no
    PreferredAuthentications password

Host kraken
    # arm
    ForwardX11 yes
    HostName kraken.ece.ncsu.edu
    User pewahle
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null
    PubkeyAuthentication no
    PreferredAuthentications password

# Globals
Host *
    # Try to use the user regulad on all systems
    User regulad
    PreferredAuthentications publickey   

# General idea of the fallback:
#    1. Try to get the agent's private key by the public key
#    2. If the private key isn't in the agent, and it exists on disk, load it. Else, crash.
Match User regulad
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_rsa3072.pub
    IdentityFile ~/.ssh/id_rsa3072  # fallback
{{- end }}
    IdentitiesOnly yes

Match User pewahle  # ncsu
{{- if not .useDummySecrets }}
    IdentityFile ~/.ssh/id_ed25519_ncstate.pub
    IdentityFile ~/.ssh/id_ed25519_ncstate  # fallback
{{- end }}
    IdentitiesOnly yes

