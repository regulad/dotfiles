# Global conventions

- Before working on files in any repository or directory, check for an `AGENTS.md` (at the repo root and in the directory containing the files being changed). If one is present, read it first and follow its conventions — it documents how to apply changes and which commands are safe to suggest.
