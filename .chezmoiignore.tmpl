.pycalc3/.venv/
.keymap-renderer/.venv/
.config/nvim/lazy-lock.json

{{- if ne .chezmoi.os "windows" }}
.chezmoiscripts/nt/*

autorun.cmd
.doskey.mac
AppData
{{- else }}
.chezmoiscripts/posix/0*
{{- end }}

{{ if not (contains "WSL2" (output "uname" "-r")) -}}
.chezmoiscripts/wsl2/*
{{ end -}}

{{- if ne .chezmoi.os "darwin" }}
Library
.chezmoiscripts/darwin/*
{{- else }}
# systemd doesn't exist on mac
.config/systemd/
{{- end }}
