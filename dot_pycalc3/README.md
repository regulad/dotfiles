# .pycalc3

This is the UV project that backs my `calc` shell command that uses iPython.

It uses Python 3.12 as opposed to 3.14 (current at time of writing) because the overflow behavior of numpy < 2 is desirable to me as a student to check side effects of operations.

